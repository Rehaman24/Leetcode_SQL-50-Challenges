-- Problem: Invalid Tweets
-- LeetCode Link: https://leetcode.com/problems/invalid-tweets/
-- Approach: Identify tweets where the content length exceeds 15 characters.

SELECT tweet_id
FROM Tweets
WHERE CHAR_LENGTH(content) > 15;
